async function handleSubmit() {
  switch (currentPage) {
    case 'Stocks':
      let rawData = await getAlphaVantageStock
    case 'Currency':
    // return 'Popular Conversions'
    case 'Government Data':
    // return 'US Government Statistics'
  }
}

export default { handleSubmit }
